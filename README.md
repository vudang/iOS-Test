## Installation
1. Clone project
    ```
    git clone https://github.com/vudang/iOS-Test.git
    ```

2. Install
    ```
    cd path_to_root_project
    pod install
    ```

3. Open `Wallet.xcworkspace`
4. Run
